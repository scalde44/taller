spring:
   datasource:
      driver-class-name: org.h2.Driver
      jdbcUrl: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
      username: sa
      password:
      hikari:
         poolName: pool

